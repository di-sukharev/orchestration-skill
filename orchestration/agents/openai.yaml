interface:
  display_name: "Orchestration"
  short_description: "Tiny tasks for a reused Luna worker, fresh reviews"
  default_prompt: "Use $orchestration to plan this feature, give one Luna worker tiny sequential tasks with precise completion criteria, and run fresh reviews with lead-agreed fixes."
